1、引用分类？
强引用 new A()
软引用 softReference，表示很有用但是又不是必须的对象，内存还有多的时候，可以放在内存里面，但是经过垃圾回收过后，内存还不够，像软引用可以被会被回收
软引用是对象回收的第二梯队，如果回收之后，内存还不够，就会发生内存溢出
弱引用，非必须的对象，它比软引用还要弱，垃圾回收时会被回收掉，用WeakReference来实现弱引用
虚引用，是最弱的引用，垃圾回收时会被回收掉，用PhantomReference来实现